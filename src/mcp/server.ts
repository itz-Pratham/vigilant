// src/mcp/server.ts
// Full implementation in Phase 7.

import chalk from 'chalk';

export async function startMcpServer(_opts: { port?: string; host?: string }): Promise<void> {
  console.log(chalk.yellow('\n  vigilant serve — MCP server available in Phase 7\n'));
}
